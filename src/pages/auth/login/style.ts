import {makeStyles} from "@mui/styles";

export const useStyles = makeStyles({
    incitingText: {
        textDecoration: 'none',
        color: '#1900D5',
        marginLeft: '10px',
        cursor: 'pointer'
    }
})